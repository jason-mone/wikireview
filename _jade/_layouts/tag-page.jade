| ---
| layout: tag
| ---
| 
| {% for post in site.posts %}{% for tag in post.tags %}{% if tag == page.tag %}
.small-6-centered.medium-6.columns
	a(href='{{post.url}}')
		paper-card(image="/assets/img/{{ post.url | remove:'.html' }}.jpg")
			h3 {{post.title}}
			time(datetime='{{post.date | date: "%Y-%m-%d"}}') {{post.date | date: "%d/%m/%Y"}}
| {% endif %}{% endfor %}{% endfor %}
paper-button(raised)
	a(href='/tags') view all tags
